# VS Code Clockify Extension

Start and stop Clockify timers based on GitHub repo context.

## Setup
- Install the extension from this folder.
- Run "Clockify: Set API Key".
- Set `clockify.workspaceId` in settings.
- Optional: set `clockify.repoMappings` for per-repo project/task selection.
